export const metadata = {
  title: "Scotland Yard Companion",
  description: "Start an investigation and keep the table's clues in sync.",
};

export default function ScotlandYardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
